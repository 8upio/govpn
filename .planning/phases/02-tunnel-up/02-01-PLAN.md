---
phase: 02-tunnel-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/keyderiv/prf.go
  - internal/keyderiv/prf_test.go
  - internal/keyderiv/keyexpansion.go
  - internal/keyderiv/keyexpansion_test.go
  - internal/keyderiv/keymethod2.go
  - internal/keyderiv/keymethod2_test.go
  - ovpn.go
  - session.go
  - test/interop/server/main.go
  - test/interop/interop_test.go
autonomous: true
requirements: [WIRE-02, WIRE-03, CTRL-04]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The TLS 1.0 PRF (`openvpn_PRF` / `tls1_P_hash`) reproduces the OpenVPN reference's own committed test vector byte-for-byte (32 bytes, `d98c8518...dab72d67`) with zero tolerance."
    - "A Key Method 2 message from a real OpenVPN 2.6 client parses field-by-field (4 reserved, 1 key-method, 48 pre_master, 32 random1, 32 random2, then four 2-byte-length-prefixed strings) without ever indexing past the buffer."
    - "The server writes its own Key Method 2 message (no pre_master, fresh random1/random2, three empty 2-byte-zero strings) and a real OpenVPN 2.6.14 client accepts it and proceeds to send `PUSH_REQUEST`."
    - "Key expansion produces 256 bytes whose per-direction slots match the reference's `KEY_DIRECTION_INVERSE` mapping for a server: encrypt cipher = bytes [128:160], encrypt implicit IV = [192:200], decrypt cipher = [0:32], decrypt implicit IV = [64:72] (D-09)."
    - "The data-channel key-direction assignment is asserted against independently-stated reference byte ranges, not against round-trip self-consistency between two instances of the same Go type."
  artifacts:
    - internal/keyderiv/prf.go
    - internal/keyderiv/keyexpansion.go
    - internal/keyderiv/keymethod2.go
    - internal/keyderiv/prf_test.go
    - internal/keyderiv/keyexpansion_test.go
    - internal/keyderiv/keymethod2_test.go
  key_links:
    - "`runHandshake` (ovpn.go) — the client's Key Method 2 payload is already buffered in `sess.conn` by Phase 1 (01-03 T-01-17); this plan is the first code that reads it, immediately after `sess.PeerCN` is assigned."
    - "`Session.dataKeys` — the derived 256-byte key expansion is stored per-session and is the single input plan 02-03's `internal/datachan.Wrapper` consumes."
    - "`sess.SessionID` / `sess.clientSessionID` — the same 8-byte control-channel session IDs feed the key-expansion PRF seed as `server_sid` / `client_sid` (ssl.c:1586-1589); no new session-ID concept is introduced."
  prohibitions:
    - statement: "No code path calls `tls.ConnectionState().ExportKeyingMaterial()` to derive data-channel keys — that is RFC 5705 export, a different construction that a real client can never reproduce."
      status: pending
    - statement: "No new entry appears in go.mod — the module stays dependency-free; the PRF is hand-written against stdlib only."
      status: pending
    - statement: "Go's unexported `crypto/tls` `prf10` implementation is not vendored, copied, or forked into this repository."
      status: pending
    - statement: "The data-channel key-direction assignment does not reuse `internal/tlscrypt.NewWrapper`'s server slot convention (server encrypt = slot 0) — the data-channel convention is the mirror opposite."
      status: pending
---

<objective>
Turn Phase 1's completed TLS handshake into a completed Key Method 2 exchange with
byte-exact data-channel key material, and close the STATE.md blocker gate
("Key Method 2 byte offsets and classic vs epoch packet-ID format must be verified
against C source before data-channel code") mechanically, with a test, before any
plan in this phase touches live data-channel traffic.

**Phase goal (ROADMAP, verbatim):** "The client brings its tunnel interface up with a
pushed IP and cipher, and encrypted IP packets round-trip between the client and the
embedder's `Session`."

*Note on MVP framing:* the ROADMAP `**Goal:**` line for Phase 2 is outcome-shaped but
not in `As a / I want to / so that` user-story form. No story is invented here — see
the planner's return note.

Purpose: this is the phase's first vertical slice. After it, a real, unmodified
OpenVPN 2.6.14 client gets past key negotiation and starts asking for its tunnel
configuration — observable progress a developer can watch in the harness log, not a
layer laid for later.

Output: `internal/keyderiv` (PRF, Key Method 2 codec, key expansion, key direction),
per-session derived key material on `Session`, and a live-client gate proving the
server's own Key Method 2 message is accepted.

## Decision IDs (from 02-CONTEXT.md)

This phase's locked decisions are labeled `D-01`..`D-16` across the four plans; the
labels are this planner's, the decisions are `02-CONTEXT.md`'s verbatim.

| ID | Decision | CONTEXT.md section |
|----|----------|--------------------|
| D-08 | `OnSession` fires only after Key Method 2 + PUSH_REPLY complete and data-channel keys are live | Session API semantics |
| D-09 | Classic non-epoch packet-ID format; byte offsets verified against `crypto.c`/`ssl.c` **before** any data-channel code | Data-channel crypto & control messages |
| D-12 | Golden vectors derived from the C reference checkout, committed with provenance, never hand-derived from RFC text alone | Data-channel crypto & control messages |
| D-13 | Internal package layout for key derivation and the data channel is Claude's discretion | Claude's Discretion |
| D-15 | How the Key Method 2 / PUSH exchange is driven over the TLS stream is Claude's discretion | Claude's Discretion |

D-13 is resolved here as `internal/keyderiv` (this plan) and `internal/datachan`
(plan 02-03), exactly as RESEARCH.md's Recommended Project Structure proposes.
D-15 is resolved here as a `bufio.Reader`-backed helper scoped to `runHandshake`'s
continuation — `internal/ctrlconn.Conn`'s Phase-1-tested behavior is not modified
(RESEARCH.md Open Question 1).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tunnel-up/02-CONTEXT.md
@.planning/phases/02-tunnel-up/02-RESEARCH.md
@.planning/phases/02-tunnel-up/02-PATTERNS.md
@.planning/phases/01-handshake/01-03-SUMMARY.md
@internal/tlscrypt/tlscrypt.go
@internal/wire/wire.go
@ovpn.go
@session.go
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: A real client's Key Method 2 message derives live data-channel keys end to end</name>

  <precondition>The pinned OpenVPN C reference checkout exists at `/Users/svenloth/dev/openvpn-reference`, branch `release/2.6`, commit `c9b790f` — every byte offset in this task is cited against it. Verify with `git -C /Users/svenloth/dev/openvpn-reference rev-parse --short HEAD`.</precondition>

  <reversibility rating="reversible">`internal/keyderiv` is an unexported internal package behind a stable `DeriveKeys`/`ReadClientKeyMethod2`/`WriteServerKeyMethod2` seam; swapping the implementation touches one directory.</reversibility>

  <read_first>
    - `/Users/svenloth/dev/govpn/ovpn.go` — current `runHandshake` (lines 358-407) and `callOnSession`; the continuation point is between `sess.PeerCN = ...` and the `OnSession` call
    - `/Users/svenloth/dev/govpn/session.go` — current `Session` struct and its field-doc-comment density (every field carries provenance + concurrency contract)
    - `/Users/svenloth/dev/govpn/internal/tlscrypt/tlscrypt.go` lines 1-20 (package-doc citation-header shape) and lines 75-82 (`keySlot`, the "prefix of a 64-byte slot" pattern)
    - `/Users/svenloth/dev/govpn/internal/wire/wire.go` lines 60-90 (sentinel parse errors + bounds-check-before-read doc comment)
    - `.planning/phases/02-tunnel-up/02-RESEARCH.md` — Pattern 1 (Key Method 2 wire layout, server perspective), Pattern 2 (`openvpn_PRF` / TLS 1.0 PRF + the committed reference test vector), Pattern 3 (key expansion seed material and slot layout)
    - `.planning/phases/02-tunnel-up/02-PATTERNS.md` — sections `internal/keyderiv/prf.go + keyexpansion.go` and `internal/keyderiv/keymethod2.go`
    - `/Users/svenloth/dev/openvpn-reference/src/openvpn/ssl.c` — `openvpn_PRF` (1476-1517), `key_direction_state_init` call site + `key_ctx_update_implicit_iv` (1519-1560), `generate_key_expansion_openvpn_prf` (1579-1630), `key_source2_read` (1890-1905), `write_empty_string` (1952-1959), `key_method_2_read` (2387-2431), `tls_process` KM2 state machine (3002-3031)
    - `/Users/svenloth/dev/openvpn-reference/src/openvpn/crypto_openssl.c` — `tls1_P_hash` (1467-1565), `ssl_tls1_PRF` (1586-1626)
    - `/Users/svenloth/dev/openvpn-reference/src/openvpn/ssl_common.h` — `struct key_source` / `struct key_source2` (116-136)
    - `/Users/svenloth/dev/openvpn-reference/tests/unit_tests/openvpn/test_crypto.c` lines 140-166 — the committed `ssl_tls1_PRF` test vector
  </read_first>

  <files>internal/keyderiv/prf.go, internal/keyderiv/keyexpansion.go, internal/keyderiv/keymethod2.go, internal/keyderiv/prf_test.go, internal/keyderiv/keymethod2_test.go, ovpn.go, session.go</files>

  <behavior>
    - `TestPRFReferenceVector`: `PRF(secret, seed, 32)` where secret is the 116-byte ASCII string `Lorem ipsum dolor sit amet, consectetur adipisici elit, sed eiusmod tempor incidunt ut labore et dolore magna aliqua.` and seed is the 66-byte ASCII string `Quis aute iure reprehenderit in voluptate velit esse cillum dolore` returns exactly the 32 bytes `d9 8c 85 18 c8 5e 94 69 27 91 6a cf c2 d5 92 fb b1 56 7e 4b 4b 14 59 e6 a9 04 ac 2d da b7 2d 67`. Byte-for-byte, no tolerance.
    - `TestPRFOutputLengthNotBlockAligned`: requesting 1, 17, 33 and 100 bytes returns exactly that many bytes and each is a prefix of the next-longer request (proves the `P_hash` A(i) chaining is truncation-correct, not per-length recomputed).
    - `TestKeyMethod2ReadRejectsTruncated`: for every prefix length from 0 to (full message length − 1) of a well-formed client message, `ReadClientKeyMethod2` returns a typed sentinel error and never panics.
    - `TestKeyMethod2ReadRejectsWrongKeyMethod`: a message whose key-method byte has a low nibble other than 2 returns `ErrKeyMethod`.
    - `TestKeyMethod2ReadRejectsOversizedString`: an options-string length field greater than 512 returns `ErrStringTooLong` before any allocation of that size.
    - `TestKeyMethod2ServerWriteLayout`: `WriteServerKeyMethod2` emits, in order, 4 zero bytes, one byte `0x02`, 32 random1 bytes, 32 random2 bytes, a 2-byte-big-endian-length-prefixed options string, then three consecutive 2-byte `0x0000` fields (username, password, peer_info) and nothing after them.
    - `TestDeriveKeysEndToEnd`: given a fixed `KeySource2` (client pre_master/random1/random2 + server random1/random2) and fixed 8-byte client/server session IDs, `DeriveKeys` returns 256 deterministic bytes; running it twice with the same input returns identical bytes; changing any single input byte changes the output.
  </behavior>

  <action>
Create the `internal/keyderiv` package. Open it with the package-doc citation header
shape `internal/tlscrypt/tlscrypt.go` lines 1-20 establishes: name the pinned
reference checkout path and commit `c9b790f5b9e8ebca5da38c22f479c31bb8d33686`, then a
bulleted list mapping each topic to `src/openvpn/<file>.c:<lines> (<C symbol>)` — at
minimum PRF seed assembly `ssl.c:1476-1517 (openvpn_PRF)`, P_hash
`crypto_openssl.c:1467-1626 (tls1_P_hash, ssl_tls1_PRF)`, key expansion
`ssl.c:1579-1630 (generate_key_expansion_openvpn_prf)`, KM2 message layout
`ssl.c:1890-1905, 2387-2431 (key_source2_read, key_method_2_read)`.

**prf.go** — implement `pHash(h func() hash.Hash, secret, seed []byte, out []byte)`
per RFC 2246 §5.6.4.1 as the reference implements it: `A(0) = seed`,
`A(i) = HMAC_h(secret, A(i-1))`, output stream `HMAC_h(secret, A(i) || seed)`
concatenated and truncated to `len(out)`. Then `PRF(secret, seed []byte, n int) []byte`:
split `secret` into `S1 = secret[:len/2]` and `S2 = secret[len/2:]` where
`len = len(secret)/2 + len(secret)%2` (the reference's odd-length one-byte overlap;
both PRF calls this project makes use even-length 48-byte secrets, but implement the
overlap correctly anyway), run `pHash` with MD5 over S1 and SHA-1 over S2 into two
`n`-byte buffers, and XOR them into the result. Import `crypto/md5` and `crypto/sha1`
with a `//nolint:gosec // G401/G505: MD5 and SHA-1 are label-mixing components of
OpenVPN's mandated RFC 2246 PRF (openvpn_PRF, ssl.c:1476-1517), not the security
boundary — that is TLS plus AES-256-GCM. Wire compatibility requires them.` annotation
directly above each import or above the file's import block, per CLAUDE.md's Development
Tools row.

Then `openvpnPRF(secret []byte, label string, clientSeed, serverSeed []byte, clientSID,
serverSID *[8]byte, n int) []byte` implementing `ssl.c:1476-1517`'s seed assembly:
seed = `label || clientSeed || serverSeed || [clientSID] || [serverSID]`, session IDs
appended only when non-nil. Do NOT confuse this seed with `pHash`'s own internal
`A(i) || seed` construction.

**keyexpansion.go** — define `type KeySource struct { PreMaster [48]byte; Random1
[32]byte; Random2 [32]byte }` and `type KeySource2 struct { Client, Server KeySource }`
mirroring `ssl_common.h:116-136`. Implement `DeriveKeys(src *KeySource2, clientSID,
serverSID *[8]byte) *Key2` as two PRF calls in this exact order:
(1) master secret, 48 bytes out, secret = `src.Client.PreMaster[:]` (ALWAYS the
client's — the server never contributes pre-master entropy), label
`OpenVPN master secret`, clientSeed = `src.Client.Random1`, serverSeed =
`src.Server.Random1`, both session IDs nil;
(2) key expansion, 256 bytes out, secret = the 48-byte master from step 1, label
`OpenVPN key expansion`, clientSeed = `src.Client.Random2`, serverSeed =
`src.Server.Random2`, clientSID = the CLIENT's control-channel session ID
(`sess.clientSessionID`), serverSID = OUR OWN (`sess.SessionID`), per `ssl.c:1586-1589`
read from the server's perspective.
Store the 256 bytes on a `Key2` type and document the slot table in a doc comment:
`keys[0].cipher = [0:64]`, `keys[0].hmac = [64:128]`, `keys[1].cipher = [128:192]`,
`keys[1].hmac = [192:256]`, with only the first 32 bytes of each cipher slot (AES-256)
and the first 8 bytes of each hmac slot (AEAD implicit IV) consumed. Zero the
intermediate master-secret buffer before returning.

**keymethod2.go** — implement `ReadClientKeyMethod2(r io.Reader) (*KeySource, *ClientOptions,
error)` reading, in order: 4 reserved bytes discarded (`ssl.c:2387-2393`
`buf_advance(buf, 4)`), 1 key-method byte whose low nibble (mask `0x0F`) must equal 2
or return `ErrKeyMethod`, 48 `pre_master`, 32 `random1`, 32 `random2`, then four
2-byte-big-endian-length-prefixed byte strings in order: options, username, password,
peer_info. A length field of 0 yields a nil string (`write_empty_string`'s counterpart,
`ssl.c:1952-1959`). Reject any length above 512 (`TLS_OPTIONS_LEN`, `ssl.h:69`) with
`ErrStringTooLong` before allocating. Use `io.ReadFull` for every fixed-length field
so a short read is an error, never a silent partial parse; declare sentinel errors
(`ErrKeyMethod`, `ErrStringTooLong`, and reuse `io.ErrUnexpectedEOF` for truncation)
in the `internal/wire` style — bounds-check before indexing, never panic on adversarial
input.

Implement `WriteServerKeyMethod2(w io.Writer, opts string) (*KeySource, error)`: fill a
fresh `KeySource` with `crypto/rand` for Random1 and Random2 only (leave PreMaster
zero — the server never sends one), then write 4 zero bytes, `0x02`, Random1, Random2,
the 2-byte-length-prefixed `opts` string (its length INCLUDES the trailing NUL, so
write `opts` plus one `0x00` byte and set the length accordingly), then three
consecutive 2-byte `0x0000` fields for username, password and peer_info. Return the
KeySource so the caller can feed it into `DeriveKeys`.

Per RESEARCH.md Pitfall 4, the options string is warning-only on mismatch
(`options_cmp_equal`, `ssl.c:2498`, gated on `--opt-verify` which this project does not
control) — send a short honest string such as
`V4,dev-type tun,link-mtu 1541,tun-mtu 1500,proto UDPv4,cipher AES-256-GCM,auth SHA1,keysize 256,key-method 2,tls-server`
and do not invest further in reproducing the reference's exact OCC format. Read the
client's options string and discard it; do not branch on peer_info (RESEARCH.md
Assumption A2).

**session.go** — add two documented unexported fields following the existing field-doc
density: `dataKeys *keyderiv.Key2` (the per-session 256-byte key expansion; per-session,
never server-global, exactly like `wrapper *tlscrypt.Wrapper`) and `clientKM *keyderiv.KeySource`
if the executor needs it retained for diagnostics. Do not add any exported method here —
`AssignedIP()` belongs to plan 02-02.

**ovpn.go** — in `runHandshake`, between `sess.PeerCN = ...` and the `if s.cfg.OnSession
!= nil` block, insert the exchange. Wrap `tlsConn` in a `bufio.Reader` scoped to this
function (D-15) — do not modify `internal/ctrlconn.Conn`. Read the client's already-buffered
KM2 message FIRST, then write the server's own, matching the reference's own
server-side state machine (`tls_process`, `ssl.c:3002-3031`: server is "Receive Key"
at `S_START`, "Send Key" at `S_GOT_KEY` — the opposite branch from the client).
Assemble a `KeySource2` from both halves, call `DeriveKeys(src, &sess.clientSessionID,
&sess.SessionID)`, and store the result on `sess.dataKeys`. On any error from the
exchange, call `sess.Close()` and return without firing `OnSession` — a session that
cannot derive keys is dead. Keep `callOnSession`'s panic-recovery wrapper untouched;
`OnSession` still fires at the end of `runHandshake` in this plan (plan 02-02 moves it
past PUSH_REPLY per D-08).

Keep the `bufio.Reader` on the `Session` (or hand it to the caller) so plan 02-02's
PUSH_REQUEST read continues from the same buffered stream — starting a second reader
over `tlsConn` would lose bytes already pulled into the first buffer.
  </action>

  <verify>
    <automated>go test -race -run 'TestPRFReferenceVector|TestPRFOutputLengthNotBlockAligned|TestKeyMethod2|TestDeriveKeysEndToEnd' ./internal/keyderiv/ &amp;&amp; go vet ./... &amp;&amp; go build ./... &amp;&amp; go test -race ./...</automated>
  </verify>

  <acceptance_criteria>
    - `go test -race -run TestPRFReferenceVector ./internal/keyderiv/` exits 0 — this is the ROADMAP Phase 2 success-criterion-3 gate and the STATE.md blocker closure; it must be green before any plan in this phase runs live data-channel traffic.
    - `go test -race ./...` exits 0 with no new failures in `internal/wire`, `internal/tlscrypt`, `internal/reliable`, `internal/ctrlconn`, or the root `ovpn` package.
    - `go vet ./...` exits 0.
    - `grep -c 'nolint:gosec' internal/keyderiv/prf.go` returns at least 1, and the annotation line names both `G401` and `G505`.
    - `git diff --stat go.mod` produces no output (the module gains no dependency).
    - `internal/keyderiv/prf.go` opens with a package-doc block naming the reference commit `c9b790f5b9e8ebca5da38c22f479c31bb8d33686` and at least three `src/openvpn/…:<lines>` citations.
  </acceptance_criteria>

  <done>`internal/keyderiv` exists with a PRF that reproduces the reference's committed 32-byte test vector exactly, a bounds-checked Key Method 2 reader/writer, and a two-stage key expansion producing 256 deterministic bytes; `runHandshake` performs the exchange and stores the derived material on the Session; the whole fast tier is green and go.mod is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The data-channel key direction is the mirror opposite of tls-crypt's — proven, not assumed</name>

  <reversibility rating="reversible">`keyDirection` is one function with one call site; the mapping it encodes is fixed by the protocol, not chosen.</reversibility>

  <read_first>
    - `/Users/svenloth/dev/govpn/internal/keyderiv/keyexpansion.go` — as written in Task 1
    - `/Users/svenloth/dev/govpn/internal/tlscrypt/tlscrypt.go` lines 130-190 — `Wrapper` doc comment and `NewWrapper`'s slot assignment, the convention that must NOT be copied
    - `.planning/phases/02-tunnel-up/02-RESEARCH.md` — Pattern 4 (key-direction slot assignment table) and Pitfall 1 in full, including its "Warning signs" paragraph
    - `.planning/phases/02-tunnel-up/02-PATTERNS.md` — the block headed "CRITICAL — do NOT copy this slot-assignment snippet"
    - `/Users/svenloth/dev/openvpn-reference/src/openvpn/ssl.c` lines 1519-1560 — `key_direction` selection and `key_ctx_update_implicit_iv`
    - `/Users/svenloth/dev/openvpn-reference/src/openvpn/crypto.c` lines 1506-1531 — `key_direction_state_init`
    - `/Users/svenloth/dev/openvpn-reference/src/openvpn/crypto_backend.h` — `MAX_CIPHER_KEY_LENGTH` (188), `MAX_HMAC_KEY_LENGTH` (506), `OPENVPN_AEAD_TAG_LENGTH` (42)
  </read_first>

  <files>internal/keyderiv/keyexpansion.go, internal/keyderiv/keyexpansion_test.go</files>

  <behavior>
    - `TestKeyDirectionServerIsInverse`: `keyDirection(true)` returns encryptIdx 1, decryptIdx 0. `keyDirection(false)` returns encryptIdx 0, decryptIdx 1.
    - `TestKeyDirectionIsOppositeOfTLSCrypt`: an explicit assertion, with a comment naming RESEARCH.md Pitfall 1, that `keyDirection(true)`'s encrypt index is NOT 0 — i.e. the value `internal/tlscrypt.NewWrapper` uses for the same boolean.
    - `TestServerSlotsMatchReferenceByteRanges`: build a `Key2` from 256 bytes where byte `i` has value `byte(i)`. Assert `ServerSlots()` returns encrypt cipher key equal to bytes 128..159, encrypt implicit IV equal to bytes 192..199, decrypt cipher key equal to bytes 0..31, decrypt implicit IV equal to bytes 64..71 — each asserted against a literal expected slice derived from the index arithmetic, never against another call of the same function.
    - `TestSlotSizes`: encrypt/decrypt cipher keys are exactly 32 bytes (AES-256) and implicit IVs are exactly 8 bytes; a 12-byte GCM nonce minus the 4-byte explicit packet ID leaves exactly the implicit-IV width.
    - `TestKey2RejectsWrongLength`: constructing a `Key2` from anything other than 256 bytes returns an error.
  </behavior>

  <action>
Add `func keyDirection(server bool) (encryptIdx, decryptIdx int)` to
`internal/keyderiv/keyexpansion.go` as a standalone, separately-testable function with
a doc comment that reproduces RESEARCH.md Pattern 4's table verbatim and states the
inversion explicitly: for `server == true` the reference selects
`KEY_DIRECTION_INVERSE`, so encrypt uses `keys[1]` and decrypt uses `keys[0]`; for a
client it is `KEY_DIRECTION_NORMAL`, encrypt `keys[0]` / decrypt `keys[1]`. Cite
`ssl.c:1519-1530` and `crypto.c:1506-1531`. The doc comment must also carry a one-line
warning that `internal/tlscrypt.NewWrapper` uses the opposite mapping for the same
`server` boolean and that copying it here is a silent interop break (RESEARCH.md
Pitfall 1).

Add `func (k *Key2) ServerSlots() DataKeys` returning a struct with four fields:
`EncryptCipher [32]byte`, `EncryptImplicitIV [8]byte`, `DecryptCipher [32]byte`,
`DecryptImplicitIV [8]byte`. Derive each from `keyDirection(true)` and the slot
arithmetic (`cipher` slot at `idx*128`, `hmac` slot at `idx*128+64`, taking the first
32 and first 8 bytes respectively) per `ssl.c:1556-1560`
(`key_ctx_update_implicit_iv(&key->encrypt, key2->keys[(int)server].hmac, …)` and
`key_ctx_update_implicit_iv(&key->decrypt, key2->keys[1-(int)server].hmac, …)`).

Write `internal/keyderiv/keyexpansion_test.go` implementing this task's stated
behavior cases above. The critical discipline, restated from RESEARCH.md Pitfall 1's "Warning signs":
a test that builds two instances of this package's own types — one `server=true`, one
`server=false` — and asserts they decrypt each other's traffic will PASS even with the
direction flipped, as long as it is flipped consistently on both sides. That is not a
sufficient test and must not be the only one. `TestServerSlotsMatchReferenceByteRanges`
must assert against index arithmetic stated independently in the test body
(`want := key256[128:160]`), so a flipped `keyDirection` makes it fail.

Add a doc-comment cross-reference in `internal/tlscrypt/tlscrypt.go`'s `Wrapper` block?
No — do not modify `internal/tlscrypt` in this plan; keep the warning inside
`internal/keyderiv` where the mistake would be made.
  </action>

  <verify>
    <automated>go test -race -run 'TestKeyDirection|TestServerSlots|TestSlotSizes|TestKey2RejectsWrongLength' -v ./internal/keyderiv/ &amp;&amp; go test -race ./...</automated>
  </verify>

  <acceptance_criteria>
    - `go test -race -run TestKeyDirectionServerIsInverse ./internal/keyderiv/` exits 0.
    - `go test -race -run TestServerSlotsMatchReferenceByteRanges ./internal/keyderiv/` exits 0, and its assertions reference the literal ranges 128:160, 192:200, 0:32 and 64:72.
    - Manually flipping the two return values of `keyDirection` makes `go test ./internal/keyderiv/` FAIL (the executor performs this flip, observes the failure, reverts it, and records the observed failure output in the plan summary — a mutation check that the assertion has teeth, mirroring `internal/tlscrypt/golden_test.go`'s tamper pattern).
    - `go test -race ./...` exits 0 after the flip is reverted.
  </acceptance_criteria>

  <done>`keyDirection` and `Key2.ServerSlots` exist with the inverted mapping documented against `ssl.c`/`crypto.c` line citations, asserted against independently-stated reference byte ranges, and a live mutation check confirms the assertion fails when the direction is flipped.</done>
</task>

<task type="auto">
  <name>Task 3: A real OpenVPN 2.6.14 client accepts the server's key exchange and asks for its config</name>

  <precondition>Docker Desktop is running locally and `docker compose version` succeeds; Phase 1's pinned interop client image (`test/interop/Dockerfile`, OpenVPN 2.6.14-0+deb12u2) builds. Without Docker this task cannot run; the fast tier from Tasks 1-2 remains the correctness gate.</precondition>

  <reversibility rating="reversible">Harness-only logging and assertions; no library behavior is decided here.</reversibility>

  <read_first>
    - `/Users/svenloth/dev/govpn/test/interop/server/main.go` — `run()` (line 59), `postHandshakeSurvival` (line 41), the structured PASS line, `observingConn`
    - `/Users/svenloth/dev/govpn/test/interop/interop_test.go` — `scenario`/`scenarioResult` types (41-78), `TestMain` (90), `runScenario` (136), `assertHandshakeCompleted` (275)
    - `/Users/svenloth/dev/govpn/ovpn.go` — `runHandshake` as modified by Task 1
    - `.planning/phases/01-handshake/01-04-SUMMARY.md` — the scenario-table pattern, `TestMain` ordering constraint, and the `go test` custom-flag ordering gotcha (package pattern must precede custom flags)
    - `.planning/phases/02-tunnel-up/02-RESEARCH.md` — Pattern 1's "Exchange order" block and Pattern 6's PUSH_REQUEST literal (`PUSH_REQUEST` plus one NUL byte, 13 bytes total)
    - `/Users/svenloth/dev/openvpn-reference/src/openvpn/push.c` lines 567 and 1089 — `buf_string_compare_advance(&buf, "PUSH_REQUEST")`
  </read_first>

  <files>test/interop/server/main.go, test/interop/interop_test.go, ovpn.go</files>

  <action>
Add an observable signal that the key exchange completed and that the real client
accepted the server's own Key Method 2 message. In `ovpn.go`, after `DeriveKeys`
succeeds, read the next message from the same buffered TLS reader and, if it is the
13-byte NUL-terminated push request literal, record that fact on the Session and log it
through the harness. In this plan the server does not yet answer it — plan 02-02 owns
PUSH_REPLY. Reading and discarding it here is safe: the reference client retransmits
its push request on its own timer (`push.c`), so plan 02-02's reply path will still see
one.

If holding the un-answered request on the Session is awkward, an equally acceptable
shape is to stop reading after `DeriveKeys` and let the harness observe key-exchange
completion instead — but then the acceptance criterion below must be satisfied by the
client's own log rather than the server's. Prefer the first shape: it proves the client
progressed past key negotiation, which is the whole point of this gate.

In `test/interop/server/main.go`, extend the structured PASS line with two new fields
derived from the Session: `km2=ok` and `push_request=seen`, alongside the existing
`peer_cn=` / `tls_version=` / `cipher_suite=` fields. Do not print any key bytes,
master secret, or key-expansion material — lengths and boolean status only (threat
T-02-03). Keep `postHandshakeSurvival`'s 2-second window; the push request arrives
inside it.

In `test/interop/interop_test.go`, add `assertKeyExchangeCompleted(t, res)` following
`assertHandshakeCompleted`'s existing shape (regexp over the captured server output,
fatal on mismatch, no `t.Skip`) asserting both new fields are present in the
`clean-small` scenario's output, and call it from `TestInteropScenarios`. Also assert
on the client's own log that it did not report a key-negotiation failure: the run must
not contain the client's TLS-key-negotiation-failed message. Follow 01-04's flag
ordering rule — the package pattern precedes any custom test flag.

Do not add a new scenario or a new compose file; reuse the existing scenario table.
  </action>

  <verify>
    <automated>go run ./cmd/gentestpki -out test/interop/pki -profile large &amp;&amp; go test -tags interop -count=1 -timeout 900s ./test/interop/ -run TestInteropScenarios -v</automated>
  </verify>

  <acceptance_criteria>
    - `make interop` exits 0 with all three scenarios (`clean-small`, `clean-large`, `lossy-large`) reporting PASS, not SKIP.
    - The `clean-small` scenario's captured server output contains both `km2=ok` and `push_request=seen` on the structured PASS line.
    - The real client's own log for that scenario does NOT contain a key-negotiation failure line, and the server process exits 0.
    - No key material appears anywhere in the harness output: the PASS line carries booleans and lengths only.
    - `go test -race ./...` (fast tier) still exits 0.
  </acceptance_criteria>

  <done>A real, unmodified OpenVPN 2.6.14 client completes the Key Method 2 exchange against the library across all three interop scenarios and proceeds to request its tunnel configuration — proven by an assertion in the scenario table, not by reading a log by hand.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → UDP socket | Unauthenticated, spoofable datagrams (Phase 1 boundary, unchanged) |
| tls-crypt unwrap → control channel | Packets past this point carry a valid tls-crypt tag but are still attacker-shaped |
| TLS peer-verified → Key Method 2 payload | The peer's certificate is CA-verified (Phase 1 gate), but every byte of the KM2 message body is still attacker-controlled content on an authenticated channel |
| `internal/keyderiv` → `Session.dataKeys` | Derived secret material crossing into per-session state that plan 02-03 will use as AEAD keys |

## STRIDE Threat Register (ASVS L1 — block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `internal/keyderiv.ReadClientKeyMethod2` | high | mitigate | Bounds-check every fixed-size field via `io.ReadFull` and every 2-byte length prefix against `TLS_OPTIONS_LEN`=512 before allocating; typed sentinel errors, never a slice-index panic. Verified by `TestKeyMethod2ReadRejectsTruncated` over every prefix length and `TestKeyMethod2ReadRejectsOversizedString`. |
| T-02-02 | Denial of Service | KM2 read inside `runHandshake` | medium | mitigate | The read happens inside the existing 60s handshake window (`Server.handshakeWindow` / `enforceHandshakeWindow`, Phase 1); a client that opens TLS and stalls mid-message is torn down by the same goroutine. No new unbounded wait is introduced and no new goroutine is added. |
| T-02-03 | Information Disclosure | derived master secret and 256-byte key expansion | high | mitigate | Key material lives only in unexported per-session fields; the intermediate master-secret buffer is zeroed after key expansion; the interop harness's diagnostic line prints booleans and lengths only, never key bytes (asserted in Task 3). |
| T-02-04 | Spoofing | data-channel key-direction assignment | high | mitigate | `keyDirection` is a standalone function asserted against independently-stated reference byte ranges plus a live mutation check (Task 2), and the live-client gate (Task 3) fails if the server's derived material does not match what the client derives. |
| T-02-05 | Repudiation | key-exchange failure paths | low | accept | A session that fails KM2 is closed and disappears; the library emits no audit log and Phase 2's scope adds none. An embedder wanting an audit trail can wrap `Config.OnSession`/`OnSessionPanic`. Revisit only if v1 grows an observability surface. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs occur this phase. `go.mod` stays at `module github.com/8upio/govpn` / `go 1.24` with no `require` directive; `git diff --stat go.mod` producing no output is an acceptance criterion of Task 1, and plan 02-04 adds a standing fast-tier gate. RESEARCH.md's Package Legitimacy Audit records zero packages proposed, so the `[ASSUMED]`/`[SUS]` human-verification path does not apply. |
</threat_model>

## Artifacts this phase produces (plan 02-01)

**New package:** `github.com/8upio/govpn/internal/keyderiv`

**New files:**
- `internal/keyderiv/prf.go`
- `internal/keyderiv/prf_test.go`
- `internal/keyderiv/keyexpansion.go`
- `internal/keyderiv/keyexpansion_test.go`
- `internal/keyderiv/keymethod2.go`
- `internal/keyderiv/keymethod2_test.go`

**New symbols in `internal/keyderiv`:**
- `func PRF(secret, seed []byte, n int) []byte`
- `func pHash(h func() hash.Hash, secret, seed, out []byte)`
- `func openvpnPRF(secret []byte, label string, clientSeed, serverSeed []byte, clientSID, serverSID *[8]byte, n int) []byte`
- `type KeySource struct { PreMaster [48]byte; Random1 [32]byte; Random2 [32]byte }`
- `type KeySource2 struct { Client, Server KeySource }`
- `type Key2` (256-byte key expansion) and `func (k *Key2) ServerSlots() DataKeys`
- `type DataKeys struct { EncryptCipher [32]byte; EncryptImplicitIV [8]byte; DecryptCipher [32]byte; DecryptImplicitIV [8]byte }`
- `func DeriveKeys(src *KeySource2, clientSID, serverSID *[8]byte) (*Key2, error)`
- `func keyDirection(server bool) (encryptIdx, decryptIdx int)`
- `func ReadClientKeyMethod2(r io.Reader) (*KeySource, *ClientOptions, error)`
- `func WriteServerKeyMethod2(w io.Writer, opts string) (*KeySource, error)`
- `type ClientOptions` (options / username / password / peer_info, read-and-discarded)
- Sentinel errors: `ErrKeyMethod`, `ErrStringTooLong`

**New unexported fields on `ovpn.Session` (session.go):**
- `dataKeys *keyderiv.Key2`
- `tlsReader *bufio.Reader` (the buffered KM2/PUSH read stream, handed to plan 02-02)
- `pushRequested bool`

**Modified:** `ovpn.go` (`runHandshake` gains the Key Method 2 continuation),
`test/interop/server/main.go` (PASS line gains `km2=` / `push_request=`),
`test/interop/interop_test.go` (`assertKeyExchangeCompleted`).

<verification>
- `go test -race ./...` exits 0 (fast tier, no Docker).
- `go vet ./...` exits 0.
- `go test -race -run TestPRFReferenceVector ./internal/keyderiv/` exits 0 — the ROADMAP criterion-3 gate; no later plan may run live data-channel traffic until this is green.
- `make interop` exits 0 with three PASSing scenarios, the `clean-small` output carrying `km2=ok push_request=seen`.
- `git diff --stat go.mod` produces no output.
</verification>

<success_criteria>
- WIRE-02 satisfied: the TLS 1.0 PRF matches the reference's own committed 32-byte test vector byte-for-byte.
- WIRE-03 satisfied: key expansion and the inverted per-direction slot/implicit-IV mapping are asserted against independently-stated reference byte ranges, with a mutation check proving the assertion has teeth.
- CTRL-04 satisfied: a real OpenVPN 2.6.14 client's Key Method 2 message parses, the server's own message is accepted, and the client proceeds to request its configuration.
- The STATE.md Phase-2 blocker ("Key Method 2 byte offsets … must be verified against C source before data-channel code") is closed by a passing test, not by assertion.
</success_criteria>

<output>
Create `.planning/phases/02-tunnel-up/02-01-SUMMARY.md` when done.
</output>
